// what is HTTP Header:- http headers are the additional data which we pass with the request.

//like in som api calls we have th mention in which format we want data
// so we have spcify that  using ACCEPT header
// we can also specify the language we want to use
// we can also specify the user agent
// we can also specify the content type
// we can also specify the content length

// check youcanhazdadjoke.com for more info